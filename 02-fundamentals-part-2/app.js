"use strict";

let hasDriversLicense = false;
const passTest = true;

if (passTest) hasDriverLicense = true; // 's' miss ho gaya (Typo)
// JS yahan error nahi dega, bas ek naya variable bana dega.
// Aap dhoondte reh jaoge ke License true kyun nahi hua.

const interface = 123;
const private = "name"
console.log(private);
